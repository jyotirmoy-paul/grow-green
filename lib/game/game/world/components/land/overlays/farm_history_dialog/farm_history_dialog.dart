import 'package:flutter/material.dart';

class FarmHistoryDialog extends StatelessWidget {
  const FarmHistoryDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
